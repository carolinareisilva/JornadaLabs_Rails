
<%= form_for @account do |f| %>
	<%if @account.errors.any? %>
		<div id="error">
			<h2>
				<%= pluralize(@account.errors.count, 'error') %> prohibited this account from begin saved:
			</h2>
			<ul>
				<% account.errors.full.messages.each do |msg| %>
					<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

<p> 
	<%= f.label :agency %><br> 
	<%= f.text_field :agency %> 
</p> 
<p> 
	<%= f.label :account_number %><br> 
	<%= f.text_field :account_number %> 
</p> 
<p> 
	<%= f.submit %> 
</p>
<% end %>